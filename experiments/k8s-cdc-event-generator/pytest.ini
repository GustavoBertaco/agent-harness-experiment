[pytest]
testpaths = tests/unit tests/integration
markers =
    unit: unit tests (no external dependencies)
    integration: integration tests (require running Kafka and Kubernetes)
addopts = -m "not integration"
